<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Practica2_1_George_Manuel"
        tools:targetApi="31">

        <!-- Pantalla principal -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Practica2_1_George_Manuel">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Pantalla New Player -->
        <activity
            android:name=".NewPlayer"
            android:exported="false"
            android:label="New Player"
            android:theme="@style/Theme.Practica2_1_George_Manuel" />

        <!-- Pantalla Preferences -->
        <activity
            android:name=".PreferencesActivity"
            android:exported="false"
            android:label="Preferences"
            android:theme="@style/Theme.Practica2_1_George_Manuel" />

        <!-- Pantalla Games -->
        <activity
            android:name=".GamesActivity"
            android:exported="false"
            android:label="Games"
            android:theme="@style/Theme.Practica2_1_George_Manuel" />

        <!-- Pantalla con Chips y BottomAppBar -->
        <activity
            android:name=".ChipsBottomActivity"
            android:exported="false"
            android:label="Material Components"
            android:theme="@style/Theme.Practica2_1_George_Manuel" />

    </application>

</manifest>
